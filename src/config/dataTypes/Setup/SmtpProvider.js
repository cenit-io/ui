import React from 'react';

export default {
    title: 'SMTP Provider',
    actions: {
        index: {
            fields: ['namespace', 'name', 'address', 'port', 'domain', 'enable_starttls_auto', 'updated_at']
        }
    }
};
