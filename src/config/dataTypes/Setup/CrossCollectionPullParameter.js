const fields = ['label', 'type', 'many', 'required', 'description', 'properties_locations'];

export default {
    title: 'Pull Parameter',
    actions: {
        new: {
            fields
        },
        edit: {
            fields
        }
    }
};
