import React from 'react';

export default {
    title: 'Flow Config',
    actions: {
        index: {
            fields: ['flow', 'active', 'auto_retry', 'notify_request', 'notify_response', 'discard_events', 'updated_at']
        }
    }
};
